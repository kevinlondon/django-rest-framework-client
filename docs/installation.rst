============
Installation
============

At the command line::

    $ easy_install django-rest-framework-client

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-rest-framework-client
    $ pip install django-rest-framework-client
